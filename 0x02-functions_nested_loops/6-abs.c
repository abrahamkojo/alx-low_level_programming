#include "main.h"

/**
 * _abs - prints 1 or 0 depending on input
 * @n: The integer to be computerd.
 *
 * Return: The absolute value of the integer..
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
